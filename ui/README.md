# Coffice Agent Deck (React UI)

Placeholder for the React sidebar app that will be hosted inside the LibreOffice
extension shell (`src/coffice/sidebar/`). The full UI (chat, tools, diff,
history, export dialog) lands in the UI bead; this directory is intentionally a
stub so the monorepo layout is complete.
